import * as mod from '../../controllers/advisoryContentController';

describe('controllers/advisoryContentController.ts', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});

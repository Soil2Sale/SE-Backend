import * as mod from '../../controllers/farmerProfileAggregateController';

describe('controllers/farmerProfileAggregateController.ts', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});

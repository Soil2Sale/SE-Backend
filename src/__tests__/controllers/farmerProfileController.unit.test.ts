import * as mod from '../../controllers/farmerProfileController';

describe('controllers/farmerProfileController.ts', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});

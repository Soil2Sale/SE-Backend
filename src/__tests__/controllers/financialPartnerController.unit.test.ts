import * as mod from '../../controllers/financialPartnerController';

describe('controllers/financialPartnerController.ts', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});

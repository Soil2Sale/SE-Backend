import * as mod from '../../controllers/governmentSchemeController';

describe('controllers/governmentSchemeController.ts', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});

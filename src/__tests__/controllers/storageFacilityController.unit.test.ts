import * as mod from '../../controllers/storageFacilityController';

describe('controllers/storageFacilityController.ts', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});

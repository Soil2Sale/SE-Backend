import * as mod from '../../../../middlewares/mail/templates/otpTemplate';

describe('middlewares/mail/templates/otpTemplate.ts', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});

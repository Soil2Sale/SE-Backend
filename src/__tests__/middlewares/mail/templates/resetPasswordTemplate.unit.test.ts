import * as mod from '../../../../middlewares/mail/templates/resetPasswordTemplate';

describe('middlewares/mail/templates/resetPasswordTemplate.ts', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
